interface RawData {
  // Define the properties of the rawData interface
  // that match the properties returned from the API
  hard: number;
  test: string;
  email: string;
  // ...
}

interface DataValues {
  text: 
}
